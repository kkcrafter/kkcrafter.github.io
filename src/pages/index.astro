---
// Root redirect → /en/
// Astro's i18n config handles this automatically with redirectToDefaultLocale: true,
// but we keep this as a fallback for direct access to the root.
return Astro.redirect('/en/');
---
